
 - No SessionStart hook auto-read — violates "navigation, not injection" (architect/context.md:70-73). The pointers in
 architect/context.md and builder/context.md are the navigation; the agent reads them when entering those modes (already an observable
 handoff).