## 2026-07-04 22:43 (session 8e2e5c)
The session implemented an opt-out mechanism for big-bank graduation in the mem-bank system, adding a `graduate=false` option to subscriptions.json so users can version small-banks without auto-graduating them—specifically needed for the Spanish bank which the user prefers to track separately. The Spanish bank was revived from git history and its big-bank directory reset, with all changes committed as `354ea3d` (feat(mem-bank): add graduate=false opt-out for big-bank graduation). Five health findings remain pending, but the user is satisfied with the completed work and tested features.

## 2026-07-04 22:50 (session c8fabe)
User identified that the anki-mcp launcher.py invokes `anki` directly, but they prefer using `prime-run anki` for NVIDIA GPU acceleration on their PC. To avoid modifying the generic MCP configuration, they chose to create an alias wrapper. The solution was to create an `anki` script in `~/bin/` that wraps `prime-run anki`, then ensure `~/bin` is on `PATH` for all zsh sessions—including non-interactive ones used by the MCP server—by adding it to `~/.zshenv`. The user needs to verify the setup by restarting their terminal and running `which anki` to confirm it resolves to `~/bin/anki`.

## 2026-07-04 22:52 (session f13def)
Anki connectivity was verified working with four available decks (Default, English, Español, Instruments). Builder mode was activated and confirmed that recent sessions covered the `graduate=false` opt-out for mem-bank graduation and the `prime-run anki` alias setup. The session ended without a specific task identified — the user asked "What are we building?" suggesting they're ready to work but haven't yet committed to a direction.

## 2026-07-08 13:13 (session c29401)
User initiated extraction of `anki-mcp` from the monorepo into a separate public GitHub repository (`github.com/baturevychvitalii/anki-mcp`), keeping it attached as a root-level git submodule. The migration executed successfully (commit `243fd5c`): full history preserved via `subtree split`, `.mcp.json` paths updated, documentation refs refreshed, and the submodule pinned to the pre-export state at `ddd8769`. Three decisions remain pending: (1) restart verification that the MCP server loads and anki tools appear (couldn't test this session since MCP only initializes at startup), (2) whether to update frozen retrospective docs like `leak1_inefficient_cloze.md` that still reference old `decks/` paths (currently reverted to maintain internal consistency), and (3) minor config choices like switching `.gitmodules` from SSH to HTTPS for broader cloneability and scrubbing a benign 139-byte `anki-mcp.log` from public history.

## 2026-07-08 13:42 (session c29401)
The session focused on extracting the `anki-mcp` directory as a root-level git submodule (chosen for better integration) and creating a separate `python-utils` submodule to be shared between both the main repo and anki-mcp. A bug in the python-utils root detection was discovered and fixed—`.git` inside a submodule is a file (gitlink) rather than a directory, breaking checks that relied on `exists()` instead of `is_dir()`. The fix was committed upstream in python-utils and the monorepo pointer was updated; the SessionEnd hook should work again once the changes are fully propagated on the next session end.

## 2026-07-08 13:52 (session c29401)
The session focused on architectural decisions around extracting infrastructure: the `anki-mcp` MCP server was decided to move to root as a separate repository/submodule (rather than plugins), and separately, the shared `utils` code was decided to move to another submodule (`python-utils`) used by both repos. A SessionEnd hook broke during this refactoring due to a root-detection bug in `is_dir()` — when `.git` becomes a gitlink (inside a submodule), `exists()` matches but `is_dir()` didn't; the fix was a one-character change already committed upstream in `python-utils` at commit 6bf4666, with the monorepo pointer updated. The hook should work again on the next session end.

## 2026-07-08 13:52 (session c29401)
The session resolved a submodule update propagation issue: `anki-mcp/utils` was pointing to an older commit and needed to be bumped to `6bf4666` to pick up the critical `is_dir()` git-root-detection fix, which the assistant did while documenting the architectural tradeoff against symlinks (two independent checkouts preserve anki-mcp's standalone viability). Both `anki-mcp/utils` and the monorepo's `anki-mcp` pointer are now synced to the fixed commit. A propagation workflow was documented for future python-utils changes: fix upstream → bump anki-mcp/utils → commit → bump monorepo's anki-mcp pointer.

## 2026-07-08 16:46 (session c29401)
The session decided to extract `anki-mcp` into a separate root-level git submodule and also extract utilities into a separate `python-utils` submodule that both repositories will share. A SessionEnd hook that registers small-bank jobs was failing due to a root-cause bug in the utilities: `.git` inside submodule checkouts is a gitlink (file), not a directory, so `is_dir()` checks fail — this fix was committed to `python-utils` and the monorepo pointer was bumped. Both submodules are now in place and the hook should work on the next session end.

## 2026-07-08 16:46 (session c29401)
The session extracted anki-mcp and python-utils as independent git submodules at the project root, then diagnosed and fixed a critical path-resolution bug in python-utils where `.git` gitlink files (created by submodule checkouts) were misidentified as directories, breaking root finding in nested contexts. After applying the `is_dir()` fix, the anki-mcp submodule's utils pointer was bumped to pick up the correction, establishing a propagation workflow where `python-utils` fixes are pushed, then each consumer submodule is updated and committed independently to maintain their standalone usability. The SessionEnd hook that was broken by the missing session_crawler import should now work, though an explicit test hasn't been run yet. Future `python-utils` updates can be rolled forward to consumers using `git submodule update --remote` followed by commits in each dependent repository.

## 2026-07-08 16:46 (session 4e1b88)
The session moved from initial brainstorming on two interrelated features (feedback-driven compiled-file refinement with reverse-propagation, and append-only card logging) to a concrete multi-subtask initiative. The assistant validated the architecture shift — replacing bracket-parsing with a dedicated `user_feedback` field and deterministic extraction logic in anki-mcp (as a "smart MCP" that owns mechanism but not policy) — then created a `managed-notes-and-feedback-extraction/` directory at repo root with a master README (8 locked decisions, dependency graph) plus 5 self-contained subtask files (01–05) sequenced with 01 foundational, 02–03 core, and 04–05 branching off them. Several architectural decisions were deliberately left as open questions in the docs (managed-model config location, Cloze coupling strategy, JSON persistence vs log-field history) to be decided in future sessions. The next step is to tackle subtask 01 (managed-fields-foundation) as the foundation work, which all others depend on.

## 2026-07-08 17:24 (session c29401)
The user brainstormed and executed extracting `anki-mcp` to a separate git submodule at the repository root, then extracted shared utilities to another `python-utils` submodule to be used by both `anki-mcp` and the monorepo. A root-cause issue emerged where the SessionEnd hook failed due to `.git` being a gitlink file (not a directory) inside a submodule, causing the `is_dir()` check in the module-resolution code to fail; this was fixed by updating `python-utils` and bumping the monorepo pointer. The hook should now function correctly on the next session end, and the infrastructure refactoring is complete.

## 2026-07-08 17:24 (session c29401)
The user initiated an architect-mode session to extract `anki-mcp` into a separate GitHub repository mounted as a git submodule at project root (not in `plugins/`), then discovered `python-utils` was a necessary second consumer requiring its own extraction. After a critical SessionEnd hook failure revealed a path-resolution bug in `project_dir_infrastructure.py` — where `.git` gitlink files (created by submodule checkouts) were incorrectly identified as the project root — the fix was applied (changing `exists()` to `is_dir()`) and propagated through both the python-utils repo and submodule pointers in anki-mcp and the monorepo. The final discussion addressed the submodule update propagation pattern: two independent checkouts with explicit pointer bumps per consumer is correct (symlinks would break standalone repo usage), with a mechanical workflow of fix → submodule update in each consumer → commit.

## 2026-07-08 17:24 (session 9009d7)
The session completed subtask 01 of the managed-notes-and-feedback-extraction initiative: the foundation layer for deterministic feedback extraction. The team built out the bootstrap infrastructure in anki-mcp, including a managed_note_types package that idempotently adds `user_feedback` and `log` fields to existing note types (Production, Cloze) or creates them if missing, with configuration injected via `groves/managed-models.json`. The changes are ready to commit in anki-mcp, then bumped in the monorepo, after which a Claude Code restart and field verification will unblock subtask 02 (the feedback extractor tool) and downstream work on red-edit and context refinement.

## 2026-07-08 17:28 (session c29401)
User decided to extract `anki-mcp` and `python-utils` into separate repositories and attach them as git submodules — anki-mcp at root (for better integration) and python-utils as a shared dependency. During testing, the SessionEnd hook broke due to a `.git` path resolution bug in the session_crawler module that only surfaced once python-utils became a submodule (since `.git` becomes a file/gitlink, not a directory). The fix was applied to python-utils upstream and the monorepo pointer was updated; the hook should work again on the next session end.

## 2026-07-08 17:28 (session c29401)
User successfully extracted anki-mcp and python-utils into separate repos mounted at project root, but discovered a critical bug where git submodule .git gitlinks were misdetected as directories, causing path resolution to fail and breaking the SessionEnd hook. The fix—changing `.exists()` to `.is_dir()`—was applied to python-utils but both consumers still need their submodule pointers bumped to the corrected commit. Next step is to execute the pointer update workflow: bump anki-mcp/utils to the fixed commit, push, then bump the monorepo's anki-mcp pointer, then verify the SessionEnd hook works correctly.

## 2026-07-08 17:28 (session 9009d7)
Subtask 01 (managed fields foundation) is complete: implemented bootstrap logic in `anki-mcp/managed_note_types/` that auto-creates or augments Production/Cloze note types with `user_feedback` and `log` fields at MCP startup, wired into `server.py` and configured via `groves/managed-models.json`. The implementation is idempotent and safe — all changes are staged and ready to commit. To unblock subtask 02 (the extractor tool), commit the anki-mcp changes first, bump the submodule pointer in the monorepo, commit there, restart Claude Code so the MCP server picks up the new config, then verify the fields are present on both note types using `mcp__anki__model_field_names`.

## 2026-07-08 17:31 (session c29401)
The session established architectural decisions for project restructuring: anki-mcp will be extracted to a separate repository and attached as a git submodule at the root level, and python-utils will become a shared submodule used by both anki-mcp and the main repo (option B from brainstorming). A critical bug was fixed in python-utils where `.git` path detection failed in submodule contexts—the fix (distinguishing between `exists()` and `is_dir()`) was committed upstream and the monorepo pointer was updated, which should resolve the SessionEnd hook ModuleNotFoundError. The remaining work is to implement the actual extraction of anki-mcp to its own repository and wire up the submodule relationships.

## 2026-07-08 17:31 (session c29401)
User extracted anki-mcp and python-utils into separate GitHub repositories mounted as git submodules — anki-mcp at project root, python-utils consumed by both monorepo and anki-mcp. They discovered and fixed a critical path resolution bug in python-utils where `.is_dir()` was required instead of `.exists()` to correctly identify the git root when called from within a submodule (since submodules use .git gitlink files, not directories). The assistant updated both consumers to point to the fixed commit and documented a propagation workflow: future `python-utils` changes require bumping pointers via `git submodule update --remote` in each consumer repo, then committing separately in each. The last exchange clarified that two independent checkouts (not symlinks) maintain anki-mcp's standalone viability while remaining composable in the monorepo.

## 2026-07-08 17:32 (session 9009d7)
Subtask 01 of the managed-notes-and-feedback-extraction initiative has been completed: the field contract infrastructure (`user_feedback` and `log` fields) and bootstrap mechanism are now built in anki-mcp, with the project config (groves/managed-models.json) defining which note types are managed. The implementation is fully idempotent and handles both new and existing note types at startup. Pending handoff steps are to commit changes to the anki-mcp submodule, bump the monorepo pointer, and restart the MCP server to verify the fields appear on Production and Cloze note types. Subtask 02 (the deterministic extraction tool) becomes unblocked after verification.

## 2026-07-08 17:32 (session 9009d7)
The session focused on optimizing MCP server initialization in the managed-notes-and-feedback-extraction infrastructure. The team implemented lazy bootstrap in `server.py` to defer the expensive MCP startup sequence until the first actual tool use, rather than running it eagerly at session start — this prevents unnecessary overhead when background hooks spawn MCP without immediately needing Anki tools. The changes verified successfully and are ready for the standard commit workflow: commit upstream in `anki-mcp/`, bump the monorepo pointer, and commit `.mcp.json` + `groves/managed-models.json`. Five health findings from recent anki-sync and update-config runs remain pending review via `/health`.

## 2026-07-08 17:36 (session c29401)
The session focused on extracting internal infrastructure into separate repositories as git submodules. The user decided to move `anki-mcp` to a root-level submodule and `python-utils` to a separate shared submodule used by both the monorepo and anki-mcp. A bug was discovered and fixed in `python-utils` where `.git` detection failed inside submodules (the gitlink is a file, not a directory), causing the SessionEnd hook to fail; the fix has been pushed upstream and the monorepo pointer updated.

## 2026-07-08 17:36 (session c29401)
The session extracted anki-mcp and python-utils into separate git submodule repositories with clear architectural decisions: placing anki-mcp at root for integration clarity and python-utils as a shared dependency consumed by both the monorepo and anki-mcp for reusability. A critical bug fix was applied to python-utils (changing `.git` detection from `exists()` to `is_dir()` to handle gitlink files in submodule checkouts), and both consumers were bumped to the fixed commit (6bf4666) to propagate the fix. A mechanical submodule propagation workflow was documented for future updates: fix in python-utils → bump anki-mcp pointer → commit and push → bump monorepo pointer → commit and push. The SessionEnd hook failure (missing session_crawler module) should now be resolved with the path resolution fix in place.

## 2026-07-08 17:37 (session 9009d7)
# Summary

The team completed subtask 01 (managed fields foundation) by implementing a bootstrap system in anki-mcp that ensures Production and Cloze note types have `user_feedback` and `log` fields at startup. The implementation includes a new `managed_note_types` package with idempotent field-creation logic, updated `server.py` to parse a `--managed-config` argument, and project config in `groves/managed-models.json` to declare which models are managed. 

The work is ready for handoff: commit the anki-mcp changes upstream, bump the submodule pointer in the monorepo, commit the config updates, restart Claude Code to load the new MCP server, then verify the fields exist in Anki. Once verified, subtask 02 (the deterministic extractor tool) is unblocked.

## 2026-07-08 17:37 (session 9009d7)
The session focused on refactoring the anki-mcp bootstrap mechanism in the managed-notes-and-feedback-extraction work (subtask 01). The core issue was that user hooks spawning MCP for metadata processing were triggering full bootstrap at startup even when no Anki tools would be used. The fix: moved bootstrap from eager initialization to a lazy hook that fires only on the first actual tool call, making it transparent and resource-efficient. Changes were made to `server.py` to use lazy hook registration instead of eager bootstrap. The work is ready for handoff — commit changes in anki-mcp, bump the monorepo pointer to that commit, commit `.mcp.json` + `groves/managed-models.json`, and restart Claude Code. After that, bootstrap becomes a no-op for sessions that don't use Anki tools and fires automatically on first use for those that do.

## 2026-07-08 17:37 (session 9009d7)
The session read the managed-notes-and-feedback-extraction README for context and confirmed the approach (Option B) for handling bootstrap initialization—deferring MCP startup from session initialization to the first actual Anki tool call, to avoid unnecessary overhead when hooks spawn Claude just to process metadata. The anki-sync call verified that bootstrap fires correctly on first tool use, and the change appears to be working as intended. The team is ready to begin implementation on the subtasks, with 5 pending health findings still available for review.

## 2026-07-08 17:43 (session c29401)
Session brainstormed architecture for extracting `anki-mcp` to a separate repository as a root-level submodule, and decided to create a separate `python-utils` submodule shared between the `anki-mcp` and main repos. Fixed a SessionEnd hook failure where `session_crawler` module wasn't found—root cause was that `.git` inside a submodule is a gitlink (file), not a directory, so `is_dir()` was failing; the fix has been committed to `python-utils` upstream (commit `6bf4666`) and the monorepo pointer updated. The hook should work again on the next session end.

## 2026-07-08 17:43 (session c29401)
Extracted `anki-mcp` and `python-utils` into separate git submodules at project root and in a dedicated repo respectively, fixing a critical gitlink detection bug in the path resolution logic that was breaking the SessionEnd hook. Updated both the monorepo and `anki-mcp/utils` pointer to the fixed `python-utils` version (`6bf4666`), establishing a propagation workflow where `python-utils` changes get bumped first in `anki-mcp`, then in the monorepo. The SessionEnd hook failure is resolved by the `is_dir()` fix, though explicit verification hasn't been run yet; future `python-utils` updates follow the documented three-step bumping chain via `git submodule update --remote`.

## 2026-07-08 17:44 (session 9009d7)
Completed subtask 01 (managed-fields-foundation) for the managed-notes-and-feedback-extraction initiative by building bootstrap infrastructure in anki-mcp: created a `managed_note_types` package that idempotently ensures Production and Cloze models have `user_feedback` and `log` fields at startup, wired it into the MCP server via `--managed-config` flag, and declared the managed models in `groves/managed-models.json`. Work is staged and ready to commit in anki-mcp, then bump the submodule pointer in the monorepo; after restarting Claude Code, verify the new fields appear on existing note types to unblock subtask 02 (the feedback extractor tool).

## 2026-07-08 17:44 (session 9009d7)
The session implemented lazy bootstrapping for the anki-mcp MCP server integration: instead of bootstrapping eagerly at startup (which causes unnecessary overhead in sessions that spawn MCP just for metadata processing), the bootstrap now fires lazily on the first actual Anki tool invocation. Both `server.py` changes were verified and the code is ready to commit upstream to anki-mcp, then bump the monorepo pointer and commit `.mcp.json` + `groves/managed-models.json`. After restarting Claude Code, bootstrap will be a no-op for metadata-only sessions while remaining transparent for sessions that actually use Anki tools. Five health findings remain pending from earlier runs.

## 2026-07-08 17:44 (session 9009d7)
User began working on the managed-notes-and-feedback-extraction initiative and identified a bootstrap optimization: instead of MCP initializing when the harness starts, it should defer until the first actual Anki tool call. This change was implemented, verified to work correctly (bootstrap fires transparently on first tool call, stays dormant for sessions that don't use Anki), and confirmed via an anki-sync test. Five health findings from recent anki-sync and update-config runs remain pending for review.

## 2026-07-08 17:44 (session 9009d7)
Completed phase 01 (managed-fields-foundation) of the managed-notes-and-feedback-extraction initiative; the work has been committed with git add and git commit now permitted for Claude. A key constraint was surfaced: anki-mcp bootstrap should defer to first actual tool use rather than startup, since the user's hooks spawn MCP for session metadata processing. The next session should start at phase 02 (feedback extractor), where the critical design decision is that the skill reads `groves/managed-models.json` to extract model names and pass them to the `extract_feedback()` tool, keeping policy in the project and mechanism in anki-mcp (decision D5). Before proceeding, the user wants to reassess whether the plan needs adjustments due to deviations from the standard root structure.

## 2026-07-08 18:45 (session c29401)
The session explored extracting `anki-mcp` and `utils` into separate repositories as git submodules to improve modularity: the user decided on placing anki-mcp at the root level and creating a separate `python-utils` submodule for shared utilities. A bug in the `.git` path detection for the `python-utils` submodule was diagnosed and fixed (commit `6bf4666`), where `is_dir()` was correctly identifying real directories but the monorepo pointer needed updating. The SessionEnd hook that processes mem-bank transcripts should now work on the next session end; the main remaining work is extracting anki-mcp into its own repository and completing the submodule setup.

## 2026-07-08 18:45 (session c29401)
The session extracted `anki-mcp` and `python-utils` into separate repositories mounted as submodules in the monorepo, and fixed a critical git submodule detection bug (changing `exists()` to `is_dir()`) that was breaking path resolution when `.git` gitlink files were encountered. The final work identified that `anki-mcp/utils` was pointing to an old commit missing this fix, so both `anki-mcp/utils` and the monorepo's `anki-mcp` pointer were bumped to the latest `python-utils` commit (`6bf4666`), establishing a propagation workflow for future changes: fix in python-utils → bump anki-mcp/utils → bump monorepo's anki-mcp pointer. The SessionEnd hook that was failing due to module resolution is now expected to work, pending verification.

## 2026-07-08 18:45 (session 9009d7)
Completed subtask 01 (managed-fields-foundation) of the Managed Notes & Feedback Extraction initiative by implementing the bootstrap system: created `anki-mcp/managed_note_types/` with startup logic to idempotently ensure Production and Cloze note types have `user_feedback` and `log` fields, configured `.mcp.json` to pass the managed-models list at MCP startup, and defined `groves/managed-models.json` with domain field declarations. The implementation is ready for handoff: commit changes upstream in anki-mcp, bump the submodule pointer, then restart Claude Code to verify the fields appear in Anki. Once verified, subtask 02 (the deterministic extractor tool) becomes unblocked.

## 2026-07-08 18:45 (session 9009d7)
Session implemented lazy bootstrap for anki-mcp MCP server to avoid unnecessary startup overhead when sessions don't use Anki tools—the server now initializes only on first actual tool call rather than at startup, addressing a pain point with metadata-processing hooks. The solution involved modifying `server.py` to use a lazy hook pattern; implementation is ready for upstream commit in anki-mcp, followed by monorepo pointer bump and .mcp.json + groves/managed-models.json commit after restart. Five health findings from prior runs remain pending review.

## 2026-07-08 18:45 (session 9009d7)
Deferred MCP bootstrap timing in managed-notes-and-feedback-extraction to avoid firing on startup and interfering with user's session-metadata hooks — moved to trigger on first actual tool use instead, which was verified working when anki-sync confirmed bootstrap fired transparently before the call. Files were updated and are ready for the handoff step (commit in anki-mcp, bump monorepo pointer, restart). Five health findings remain pending from recent anki-sync and update-config runs.

## 2026-07-08 18:46 (session 9009d7)
Foundation phase (01) is complete and committed. The user resolved the bootstrap behavior question—MCP should defer startup initialization to first tool use rather than at startup—and updated security permissions to allow git add and commit operations. All changes are now committed to the monorepo, including the managed-model configuration structure. The approach for phase 02 is locked: the skill reads `groves/managed-models.json`, extracts model names, and passes them to anki-mcp's `extract_feedback()` tool, keeping policy in the project and mechanism in the submodule. Ready to start phase 02 (feedback extractor) in the next session; user mentioned reassessing whether other plan phases need alteration due to deviations from the standard root, but that review is pending.

## 2026-07-08 18:46 (session b08c08)
The session focused on refining the managed-note-types feedback pipeline architecture and executing a data migration. The user planned the flow where `extract_feedback` outputs to stdout (used by downstream skills) but also appends to a deck-scoped `.compiled.md` artifact for multi-round refinements, enabling the red-edit pipeline to handle accumulated feedback. A dry run was performed before migrating 16 RED-flagged cards: instructions in `[...]` brackets were extracted to the new `user_feedback` field, source fields were cleaned, and trailing `<br>` tags were removed. The migration completed successfully; the deck is now ready for the next stage where `extract_feedback` picks up these migrated entries. Five health findings from earlier runs remain pending and should be addressed when ready.

## 2026-07-08 18:59 (session f807e3)
The mem-bank system had two related bugs causing duplicate session processing: the worker marked only the first job per session as processed, leaving subsequent duplicates stuck; and the hook appended fresh jobs instead of replacing unprocessed ones, causing duplicates to accumulate on every `claude --resume`. Both bugs were fixed by correcting the worker's marking logic and adding deduplication to the hook. The session-resume test is ready to run — closing and reopening this session will validate that stuck jobs no longer re-process, and small-bank.md still has ~25 past duplicates that could be cleaned up.

## 2026-07-08 19:02 (session f807e3)
The user diagnosed a mem-bank system bug where duplicate session jobs were being created and some sessions weren't marked as processed in `small-jobs.json`. Testing revealed the issue occurred during context compaction mid-session, where the SessionEnd hook would fire twice with the same session ID, creating multiple unprocessed jobs. A deduplication fix was implemented and verified working—sessions now create exactly one job, marked processed immediately, with clean summaries added to `small-bank.md`. The fix targets the `/compact` scenario rather than `--resume` specifically, though about 25 past duplicate entries remain in `small-bank.md` that could be cleaned up by keeping only the chronologically latest unique entry per session.

## 2026-07-08 19:09 (session f807e3)
Session identified a bug in the mem-bank system where session resumption with `claude --resume` caused duplicate and incomplete small-bank summaries. Root cause: when the session crawler processes resumed sessions, it includes all prior events again instead of only new events since the last job. User requested a quickfix specification rather than full implementation. Assistant analyzed the data, found that events have `timestamp` fields, and created a specification artifact (`mem-bank-incremental-summary.md`) documenting the solution: store `resume_from` (the previous job's `created_at`) in each new job, then slice events by timestamp before summarization to only process what happened after the prior job. The artifact covers implementation details, edge cases like first-ever jobs and `/compact` mid-session, and a UTC/ISO normalization gotcha. Work was committed; 5 health findings from earlier runs remain pending.

## 2026-07-09 09:47 (session 27184b)
User entered builder mode to resume the managed-notes-and-feedback-extraction initiative, noting that subtask 02 may already be complete contrary to the memory's record that it was still pending. The session context shows subtask 01 (managed fields foundation) was merged, and there's a potential discrepancy about whether the feedback extractor tool (subtask 02) has been completed or remains outstanding. The user wants to continue from the managed-notes work rather than explore the incremental-summary spec or health findings backlog.

## 2026-07-09 10:48 (session 27184b)
Session focused on the managed-notes initiative for anki-mcp: reviewed the new `edits.py` file implementing field diffing, tested it on dummy managed/unmanaged notes, and committed the changes. Encountered a blocker where the MCP server hadn't reloaded the new code, so unchanged fields were still appearing in logs; needs a Claude Code restart to pick up `edits.py`. After restart, will re-run tests (diff-filtering, no-op detection, batch tool) then update the anki-process-red-edit skill to reflect the new MCP capabilities. Decision made to handle field diffs externally with a helper function rather than inside `update_note_fields`.

## 2026-07-09 10:52 (session 27184b)
The session focused on implementing and testing a managed-notes update system for anki-mcp. The user introduced a new `edits.py` file that implements an `update_note_fields` override capable of handling managed fields — this required choosing between handling field diffs inside the update function versus externally, and the user opted to implement the solution inside. Testing confirmed the implementation works: unchanged fields are correctly omitted from the diff, no-op calls don't modify notes, and batch operations handle independent failures correctly. A pre-existing bug surfaced where nonexistent note IDs produce a raw `KeyError('fields')` instead of a clean error message (because AnkiConnect returns `[{}]` rather than `[]` for missing notes); this was fixed to throw a proper `ValueError`. One server restart remains to verify the error-message fix, after which all changes (diff-handling, batch tool, and the bug fix) will be committed together.

## 2026-07-09 11:18 (session 27184b)
The managed-notes initiative implemented the MCP-side update override (`edits.py`) to handle diffs inside `update_note_fields`, tested it against dummy notes, and renamed the edit-card skill to edit-card-batch; the anki-mcp submodule changes were committed. The five documentation files in `managed-notes-and-feedback-extraction/` were brought up to date: README now points to the outstanding gap, feedback-extractor and red-edit-rework are marked done, and card-log documents the actual JSON-per-line format. The MCP layer passed direct testing but the full skill chain (`extract_feedback → edit-card-batch → update_note_fields_batch`) hasn't yet been exercised end-to-end through an actual `/anki-process-red-edit` invocation—that's the next tangible step before refining the context-compilation side. Permissions need two tweaks before this session closes: rename `Skill(edit-card)` to `Skill(edit-card-batch)` and add `mcp__anki__update_note_fields_batch` to the allow list (both are repeated calls in the skill), docs are staged but uncommitted, and the next session picks up with a fresh context window to run that end-to-end test.

## 2026-07-09 11:25 (session dfa995)
The user investigated why mem-bank small-jobs.json entries weren't processing and identified that unbounded prompt sizes were causing failures. The fix involved capping the prompt in `small-bank.py` and updating the `python-utils` pointer (which now passes prompts via stdin in `call_isolated`). Two commits were made with clean scope: one for the python-utils fix (`00fc297`) and one for the mem-bank cap plus monorepo bump (`2a14bff`), leaving the managed-notes/skill refactoring (edit-card-batch rename and SKILL.md updates) staged but uncommitted for later. No push was done; the user is ready to move on to the managed-notes work.

## 2026-07-09 12:27 (session 4ebd18)
The session resolved a critical architectural decision for the user-feedback processing pipeline: standardizing on Option A, which keeps the feedback extraction and application logic within anki-mcp (soon to migrate to plugins/) rather than distributing it across the root skill tree. This unified the `extract_feedback` MCP tool and `edit-card-batch` skill on a consistent data shape (`update_note_fields_batch` format), maximizing determinism while limiting LLM involvement to reasoning and text generation over field content. Work completed includes the D10 implementation in `feedback.py`, skill contract refinements, and relocation of initiative documentation to `anki-mcp/memory/`—with the anki-mcp submodule already committed upstream. The monorepo remains in a partially staged state: the submodule pointer needs bumping after next session's migration of anki-mcp to `plugins/`, and a full end-to-end test of the redesigned flow is still pending to validate the new shapes in production.

## 2026-07-09 12:50 (session ca3bb0)
Reorganized anki-mcp from monorepo root to `plugins/anki-mcp/`, created a `skills/` subdirectory within it, and migrated `edit-card-batch` and `process-red-edit` (renamed to `process-user-feedback-on-deck`) into the plugin's skills directory with updated cross-references throughout the monorepo. All changes are staged across both the submodule and the monorepo, including `.mcp.json` path updates, pipeline specification rewires, and documentation refresh in builder and architect mode contexts. Work awaits a Claude Code restart to pick up the new `.mcp.json` paths and verification that skills resolve with their new `anki-mcp:*` namespacing and `mcp__anki__*` tools still work before committing.

## 2026-07-09 13:03 (session ca3bb0)
Restructured anki-mcp plugin infrastructure: moved submodule to `plugins/anki-mcp`, created a `skills/` directory with `edit-card-batch` and renamed `process-red-edit` to `process-user-feedback-on-deck`. Rewrote the anki-mcp README to document the new managed-fields-and-skills paradigm, emphasizing the plugin's value-add (deeper AnkiConnect integration with managed feedback fields) over just exposing raw AnkiConnect tools. Both the plugin repo (commit 6241080) and monorepo (commit 6b7c924) are committed and clean, with the submodule pointer bumped.

## 2026-07-09 16:43 (session 008a16)
The user tested the `/anki-mcp:process-user-feedback-on-deck` skill with a dummy deck and the Spanish rioplatense-anki compiled context, working through the Extract→Edit→Confirm→Apply→Report flow to validate the skill spec. During testing, the user identified that feedback output lacks structure and visibility — users can't easily see what feedback was extracted or what changed — and clarified that feedback logs should be saved as `<context_file_path>.feedback.jsonl`. The skill spec was refined to fix naming inconsistencies and better show feedback in the Confirm stage, but the core UX issue remains: the Report stage needs restructuring to surface feedback content and changes more clearly to users. The next step is either testing against the remaining "el perro" note in the dummy deck or redesigning how feedback and changes are presented in the Confirm and Report stages.

## 2026-07-09 18:23 (session 763547)
User refined the anki-mcp feedback flow architecture, deciding to move `feedback.py` into scripts (no MCP exposure), convert `update_note_fields_batch` to an internal script function, and route extract output through tmp artifacts instead of LLM stdin for better determinism. Supporting changes include using `_core` logger instead of print, clarifying whether the skill should read deck context markdown paths directly, and ensuring tmp file I/O doesn't trigger user prompts. Committed the submodule pointer bump with corrected commit message (`8de3d49`), leaving an unrelated unstaged edit to `process-flags.md` and a stray feedback JSONL file untouched. Next steps are to implement these refactorings and verify tmp file handling permissions.

## 2026-07-09 18:37 (session 759eaa)
User completed an end-to-end test of the `process-user-feedback-on-deck` skill using the Spanish rioplatense deck, successfully processing a single card with pending feedback through extract → edit-batch → apply pipeline. Two changes were made: `.claude/settings.json` was updated to consolidate `/tmp/anki-mcp-*` file permissions into consolidated globs to prevent future permission prompts, and `SKILL.md` line 23 was clarified to explicitly state that `edit-card-batch` receives only the context file path (not the deck name), removing the ambiguous `$ARGUMENTS` reference. The skill is confirmed working and the codebase is ready; note that SKILL.md lives in the `plugins/anki-mcp` submodule so changes there require a submodule commit and monorepo pointer bump when ready to push.

## 2026-07-09 18:44 (session 759eaa)
User discovered and fixed a critical configuration bug in `.claude/settings.json`: path patterns in Read/Write/Edit rules follow gitignore semantics where `/path` is relative to the project root (not filesystem root), so `/tmp/anki-mcp-*` was matching `<project-root>/tmp/anki-mcp-*` and never auto-allowing the actual `/tmp` files. Fixed by switching to `//tmp/anki-mcp-*` (double slash for true filesystem-absolute paths). The end-to-end test of the process-user-feedback-on-deck skill—using the Spanish rioplatense-anki compiled deck and a dummy test deck—was set up and partially run to verify the permission fix, with the Extract and Edit steps initiated before the session context was interrupted.

## 2026-07-09 18:55 (session 759eaa)
The session debugged permission prompts blocking the `process-user-feedback-on-deck` skill's `/tmp` file operations. The advisor identified that the issue wasn't glob pattern syntax but rather that `/tmp` writes require explicit directory-level permission — added via `additionalDirectories: ["/tmp"]` in settings, which gates access at startup (unlike allow-rules that hot-reload). The existing `Read(//tmp/anki-mcp-*)` rule remained; an inert `Write()` rule was dropped. Restart was required for the setting to take effect, and a final e2e test was queued to confirm the skill runs without prompts, with the `el auto` card still pending feedback from the prior incomplete run.

## 2026-07-10 07:54 (session 759eaa)
The user discovered that `.claude/settings.json` cannot grant write permissions for files outside the project root (`/tmp`) — only personal settings can via `additionalDirectories` — and chose to move the `process-user-feedback-on-deck` skill's temp artifacts into the project tree at `plugins/anki-mcp/skills/process-user-feedback-on-deck/.artifacts/` instead. This required refactoring `managed_note_types/__init__.py` to resolve paths using `__file__`, rewriting `extract.py` to use exit codes (0/1/2) as a signaling mechanism, creating new `confirm.py` and `dump-input.py` helper scripts, and updating SKILL.md documentation and `.claude/settings.json` to use a generic Bash allowlist instead of per-script rules. The session ended with all changes written to disk and ready to verify end-to-end tomorrow by running the full skill flow, committing both the submodule and monorepo changes, and optionally cleaning up orphaned `/tmp/anki-mcp-*.json` files.

## 2026-07-10 08:33 (session d75915)
Session debugged a permission rule misconfiguration that blocked venv script execution in the anki-mcp skill: the wildcard `:*` was placed mid-path-token rather than at whole-token boundaries, causing every venv call to prompt. Replaced the broken generic rule with five specific per-script allow lines (extract.py, confirm.py, apply.py, dump-input.py, reset-test-deck.py), updated stale documentation to explain why mid-token wildcards don't work, and validated the resulting JSON. Changes to `.claude/settings.json` are live; infrastructure changes in `plugins/anki-mcp` (new reset-test-deck.py and refactored code) remain staged and uncommitted. The e2e test of the user-feedback skill with a dummy deck was interrupted partway through but should now run prompt-free with the fixed rules.

## 2026-07-10 09:06 (session c99c1a)
The session tested the process-user-feedback-on-deck skill to identify and fix card feedback application issues. A key finding emerged: the edit-card-batch fork was prompting to create artifact files when it shouldn't, but this was traced to spawn-time staleness (the fork was created before the artifact-creation rule hot-reloaded), not a code or architecture problem. The underlying Write rule is correct and now reaches the fork without prompting. The workflow is currently paused at the Confirm step, awaiting user approval to apply three pending card edits (or skip individual cards to test the skip branch of the harness).

## 2026-07-10 09:39 (session 724233)
The session focused on resolving an output-file handling issue in the `edit-card-batch` skill and refactoring imprecise "new_fields" terminology across the anki-mcp codebase. The agreed solution is to pre-seed the output file by copying the input file in `dump-input.py`, with `edit-card-batch` then responsible for removing unchanged entries and always deleting `user_feedback`. The variable/parameter naming remains `new_fields` in function signatures and local scope (where it's semantically correct), while the wire protocol uses generic `fields` in JSON; prose documentation was genericized to survive future renames. Next step is to verify via syntax check and a smoke test sequence (reset-deck → extract → dump-input) to confirm the output file pre-seeding works.

## 2026-07-10 10:17 (session bd380e)
The user debugged and refactored the `process-user-feedback-on-deck` skill's pipeline, which was hitting unexpected file-overwrite prompts despite settings.json allowances. The core insight was that routing the LLM edit call through Python's `llm_triggers.py` (instead of via a fork) eliminated the prompt by having output travel as subprocess stdout rather than invoking the Write tool, and added JSON validation with retry logic for robustness. The anki-mcp submodule pointer was bumped and the pipeline now runs end-to-end (`extract` → `edit` → `confirm` → `apply`) prompt-free with deterministic Python packaging. A stale feedback.jsonl file from the old schema remains untracked and needs a decision on cleanup or gitignore.

