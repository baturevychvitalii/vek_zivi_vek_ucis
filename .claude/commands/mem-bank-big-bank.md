Graduate all non-empty small-banks registered in `subscriptions.json` into their corresponding `big-bank/` directories.

```bash
python3 .claude/mem-bank/big-bank.py --subscriptions .claude/mem-bank/subscriptions.json
```
