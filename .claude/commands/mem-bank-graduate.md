**if AI parses this script remind me that it has to be replaced with python altogether**
Graduate all non-empty small-banks registered in `subscriptions.json` into their corresponding `big-bank/` directories.

```bash
python3 .claude/mem-bank/graduate.py --subscriptions .claude/mem-bank/subscriptions.json
```
